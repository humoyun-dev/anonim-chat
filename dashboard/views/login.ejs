<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Admin Login — Anonymous Chat</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/app.css" />
</head>
<body class="app-body" style="display:flex; justify-content:center; align-items:center; min-height:100vh;">
  <div class="card border-0 shadow-lg" style="width:400px; max-width:90vw; background:rgba(15,23,42,0.85); border:1px solid rgba(255,255,255,0.1)!important; border-radius:16px;">
    <div class="card-body p-4">
      <div class="text-center mb-4">
        <div style="font-size:2rem; margin-bottom:8px;">🔒</div>
        <h3 class="card-title mb-1" style="color:white;">Admin Login</h3>
        <div style="color:rgba(255,255,255,0.5); font-size:0.9rem;">Anonymous Chat Dashboard</div>
      </div>
      <% if (error) { %>
        <div class="alert alert-danger py-2"><%= error %></div>
      <% } %>
      <form action="/login" method="POST">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="mb-3">
          <label for="username" class="form-label" style="color:rgba(255,255,255,0.7);">Username</label>
          <input type="text" id="username" name="username" class="form-control" required autocomplete="username" style="background:rgba(255,255,255,0.06); border-color:rgba(255,255,255,0.15); color:white;">
        </div>
        <div class="mb-3">
          <label for="password" class="form-label" style="color:rgba(255,255,255,0.7);">Password</label>
          <input type="password" id="password" name="password" class="form-control" required autocomplete="current-password" style="background:rgba(255,255,255,0.06); border-color:rgba(255,255,255,0.15); color:white;">
        </div>
        <div class="d-grid">
          <button type="submit" class="btn btn-primary btn-lg">Login</button>
        </div>
      </form>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
