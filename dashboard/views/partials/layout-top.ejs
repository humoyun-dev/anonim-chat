<!doctype html>
<html lang="uz">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || "Admin Dashboard" %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/app.css" />
  </head>
  <body class="app-body">
    <div class="app-shell">
      <aside class="app-sidebar" data-sidebar>
        <div class="sidebar-brand">
          <div class="brand-title">Anonim Chat</div>
          <div class="brand-sub">Admin</div>
        </div>
        <nav class="sidebar-nav">
          <a
            class="sidebar-link <%= active === 'chat' ? 'active' : '' %>"
            href="/chat"
            >Chat</a
          >
          <a
            class="sidebar-link <%= active === 'dashboard' ? 'active' : '' %>"
            href="/dashboard"
            >Analytics</a
          >
          <a
            class="sidebar-link <%= active === 'users' ? 'active' : '' %>"
            href="/users"
            >Users</a
          >
          <a
            class="sidebar-link <%= active === 'messages' ? 'active' : '' %>"
            href="/messages"
            >Messages</a
          >
          <a
            class="sidebar-link <%= active === 'sessions' ? 'active' : '' %>"
            href="/sessions"
            >Sessions</a
          >
          <a class="sidebar-link" href="/export/messages">Export CSV</a>
        </nav>
        <div class="sidebar-footer">
          <a class="sidebar-link logout" href="/logout">Logout</a>
        </div>
      </aside>

      <div class="app-main">
        <header class="app-topbar">
          <button
            type="button"
            class="sidebar-toggle"
            aria-label="Toggle sidebar"
            data-sidebar-toggle
          >
            ☰
          </button>
          <div class="topbar-title"><%= title || "" %></div>
          <div class="topbar-spacer"></div>
          <a class="topbar-link" href="/logout">Logout</a>
        </header>

        <div class="app-content">

